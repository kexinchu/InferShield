Invalid for the paper. Concurrent LPM true-vs-false races let the
true continuation hit victim KV (or an in-batch prefix) so Strict
ASR collapsed to vanilla (~94.8%). Isolation only shows when each
probe is a fresh principal, sequential, never equal to the victim.
